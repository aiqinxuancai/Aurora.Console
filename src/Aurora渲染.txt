.版本 2

.程序集 Aurora渲染

.子程序 标记输出行, , , 解析样式标记并输出（自动换行）
.参数 标记文本, 文本型

标记输出 (标记文本)
写控制台 (#换行符)

.子程序 标记输出, , , 解析样式标记并输出（不自动换行）
.参数 标记文本, 文本型
.局部变量 数据, 字节集
.局部变量 总数, 整数型
.局部变量 指针, 整数型
.局部变量 段起点, 整数型
.局部变量 属性, 整数型
.局部变量 属性栈, 整数型, , "0"
.局部变量 当前字节, 整数型
.局部变量 扫描, 整数型
.局部变量 标签终点, 整数型
.局部变量 标签, 文本型
.局部变量 令牌, 文本型, , "0"
.局部变量 k, 整数型
.局部变量 令牌文本, 文本型
.局部变量 新色, 整数型
.局部变量 栈顶, 整数型
.局部变量 已转义, 逻辑型

数据 ＝ 到字节集 (标记文本)
总数 ＝ 取字节集长度 (数据)
属性 ＝ #控制台_灰
指针 ＝ 1
段起点 ＝ 1
.判断循环首 (指针 ≤ 总数)
    当前字节 ＝ 数据[指针]
    .如果 (当前字节 ＝ 91)
        已转义 ＝ 假
        .如果真 (指针 ＜ 总数)
            .如果真 (数据[指针 ＋ 1] ＝ 91)
                已转义 ＝ 真
            .如果真结束

        .如果真结束
        .如果 (已转义)
            .如果真 (指针 ＞ 段起点)
                输出着色文本 (取文本中间 (标记文本, 段起点, 指针 － 段起点), 属性)
            .如果真结束
            输出着色文本 (“[”, 属性)
            指针 ＝ 指针 ＋ 2
            段起点 ＝ 指针
        .否则
            标签终点 ＝ 0
            扫描 ＝ 指针 ＋ 1
            .判断循环首 (扫描 ≤ 总数)
                .如果真 (数据[扫描] ＝ 93)
                    标签终点 ＝ 扫描
                    跳出循环 ()
                .如果真结束
                .如果真 (数据[扫描] ＝ 91)
                    跳出循环 ()
                .如果真结束
                .如果真 (扫描 － 指针 ＞ 40)
                    跳出循环 ()
                .如果真结束
                扫描 ＝ 扫描 ＋ 1
            .判断循环尾 ()
            .如果 (标签终点 ＞ 0 且 标签终点 ≠ 指针 ＋ 1)
                .如果真 (指针 ＞ 段起点)
                    输出着色文本 (取文本中间 (标记文本, 段起点, 指针 － 段起点), 属性)
                .如果真结束
                标签 ＝ 取文本中间 (标记文本, 指针 ＋ 1, 标签终点 － 指针 － 1)
                .如果 (寻找文本 (标签, “/”, , 假) ＝ 1)
                    .如果 (取数组成员数 (属性栈) ＞ 0)
                        栈顶 ＝ 取数组成员数 (属性栈)
                        属性 ＝ 属性栈[栈顶]
                        删除成员 (属性栈, 栈顶, 1)
                    .否则
                        属性 ＝ #控制台_灰
                    .如果结束

                .否则
                    加入成员 (属性栈, 属性)
                    令牌 ＝ 分割文本 (标签, “ ”, )
                    .计次循环首 (取数组成员数 (令牌), k)
                        令牌文本 ＝ 到小写 (令牌[k])
                        .如果真 (令牌文本 ＝ “bold” 或 令牌文本 ＝ “strong”)
                            属性 ＝ 位或 (属性, #控制台_高亮)
                        .如果真结束
                        .如果真 (令牌文本 ＝ “invert” 或 令牌文本 ＝ “reverse”)
                            属性 ＝ 位或 (左移 (位与 (属性, 15), 4), 右移 (位与 (属性, 240), 4))
                        .如果真结束
                        .如果真 (令牌文本 ＝ “default”)
                            属性 ＝ #控制台_灰
                        .如果真结束
                        .如果真 (取文本左边 (令牌文本, 3) ＝ “bg_”)
                            新色 ＝ 名字到颜色 (取文本中间 (令牌文本, 4, 取文本长度 (令牌文本) － 3))
                            .如果真 (新色 ≥ 0)
                                属性 ＝ 位或 (位与 (属性, 15), 左移 (新色, 4))
                            .如果真结束

                        .如果真结束
                        .如果真 (取文本左边 (令牌文本, 1) ＝ “#”)
                            新色 ＝ 十六进制色到属性 (令牌文本)
                            .如果真 (新色 ≥ 0)
                                属性 ＝ 位或 (位与 (属性, 248), 新色)
                            .如果真结束

                        .如果真结束
                        .如果真 (取文本左边 (令牌文本, 1) ≠ “#” 且 取文本左边 (令牌文本, 3) ≠ “bg_”)
                            新色 ＝ 名字到颜色 (令牌文本)
                            .如果真 (新色 ≥ 0)
                                属性 ＝ 位或 (位与 (属性, 248), 新色)
                            .如果真结束

                        .如果真结束

                    .计次循环尾 ()
                .如果结束
                指针 ＝ 标签终点 ＋ 1
                段起点 ＝ 指针
            .否则
                指针 ＝ 指针 ＋ 1
            .如果结束

        .如果结束

    .否则
        .如果 (当前字节 ≥ 129 且 指针 ＜ 总数)
            指针 ＝ 指针 ＋ 2
        .否则
            指针 ＝ 指针 ＋ 1
        .如果结束

    .如果结束

.判断循环尾 ()
.如果真 (段起点 ≤ 总数)
    输出着色文本 (取文本中间 (标记文本, 段起点, 总数 － 段起点 ＋ 1), 属性)
.如果真结束
设置控制台属性 (#控制台_灰)

.子程序 输出着色文本, , , 以组合颜色属性输出文本（不换行）
.参数 文本, 文本型
.参数 属性值, 整数型

设置控制台属性 (属性值)
写控制台 (文本)

.子程序 设置控制台属性, 逻辑型, , 直接按组合值设置控制台文本属性，成功返回真
.参数 属性值, 整数型

返回 (SetConsoleTextAttribute (取输出句柄 (), 属性值) ≠ 0)

.子程序 输出分隔线, , , 输出 Aurora 风格分隔线，可带居中标题
.参数 标题, 文本型, 可空
.参数 宽度, 整数型, 可空
.参数 颜色, 整数型, 可空
.局部变量 线宽, 整数型
.局部变量 标题宽, 整数型
.局部变量 左段, 整数型
.局部变量 右段, 整数型
.局部变量 色值, 整数型

色值 ＝ #控制台_深灰
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
.如果 (是否为空 (宽度) 或 宽度 ≤ 0)
    线宽 ＝ 取控制台宽度 ()
.否则
    线宽 ＝ 宽度
.如果结束
.如果真 (线宽 ＜ 1)
    线宽 ＝ 1
.如果真结束
.如果 (是否为空 (标题) 或 标题 ＝ “”)
    设置控制台属性 (色值)
    写控制台 (取重复文本 (线宽, “─”))
    写控制台 (#换行符)
    设置控制台属性 (#控制台_灰)
.否则
    标题宽 ＝ 显示宽度 (标题)
    .如果 (标题宽 ＋ 2 ≥ 线宽)
        左段 ＝ 0
        右段 ＝ 0
    .否则
        左段 ＝ 取整 ((线宽 － 标题宽 － 2) ÷ 2)
        右段 ＝ 线宽 － 标题宽 － 2 － 左段
    .如果结束
    设置控制台属性 (色值)
    写控制台 (取重复文本 (左段, “─”))
    .如果真 (左段 ＞ 0)
        写控制台 (“ ”)
    .如果真结束
    设置控制台属性 (#控制台_白)
    写控制台 (标题)
    设置控制台属性 (色值)
    .如果真 (右段 ＞ 0)
        写控制台 (“ ”)
    .如果真结束
    写控制台 (取重复文本 (右段, “─”))
    写控制台 (#换行符)
    设置控制台属性 (#控制台_灰)
.如果结束


.子程序 输出表格, , , 输出 Aurora 风格边框表格（行数据按行优先展平为一维数组）
.参数 列数, 整数型
.参数 表头, 文本型, 数组
.参数 行数据, 文本型, 数组
.参数 颜色, 整数型, 可空
.参数 风格, 整数型, 可空
.参数 对齐数组, 整数型, 数组
.局部变量 宽数组, 整数型, , "0"
.局部变量 j, 整数型
.局部变量 i, 整数型
.局部变量 行数, 整数型
.局部变量 色值, 整数型
.局部变量 数值, 整数型
.局部变量 风格值, 整数型

.如果真 (列数 ≤ 0)
    返回 ()
.如果真结束
色值 ＝ #控制台_深灰
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
风格值 ＝ 1
.如果真 (是否为空 (风格) ＝ 假 且 风格 ≥ 1)
    风格值 ＝ 风格
.如果真结束
重定义数组 (宽数组, 假, 列数)
.变量循环首 (1, 列数, 1, j)
    数值 ＝ 0
    .如果真 (j ≤ 取数组成员数 (表头))
        数值 ＝ 显示宽度 (表头[j])
    .如果真结束
    宽数组[j] ＝ 数值
.变量循环尾 ()
行数 ＝ 取整 (取数组成员数 (行数据) ÷ 列数)
.变量循环首 (1, 行数, 1, i)
    .变量循环首 (1, 列数, 1, j)
        .如果真 ((i － 1) × 列数 ＋ j ≤ 取数组成员数 (行数据))
            数值 ＝ 显示宽度 (行数据[(i － 1) × 列数 ＋ j])
            .如果真 (数值 ＞ 宽数组[j])
                宽数组[j] ＝ 数值
            .如果真结束

        .如果真结束

    .变量循环尾 ()
.变量循环尾 ()
表格边线 (取边框符号 (风格值, 3), 取边框符号 (风格值, 4), 取边框符号 (风格值, 5), 取边框符号 (风格值, 1), 宽数组, 色值)
表格内容行 (表头, 1, 列数, 宽数组, #控制台_白, 取边框符号 (风格值, 2), 对齐数组)
表格边线 (取边框符号 (风格值, 6), 取边框符号 (风格值, 7), 取边框符号 (风格值, 8), 取边框符号 (风格值, 1), 宽数组, 色值)
.变量循环首 (1, 行数, 1, i)
    表格内容行 (行数据, (i － 1) × 列数 ＋ 1, 列数, 宽数组, #控制台_灰, 取边框符号 (风格值, 2), 对齐数组)
.变量循环尾 ()
表格边线 (取边框符号 (风格值, 9), 取边框符号 (风格值, 10), 取边框符号 (风格值, 11), 取边框符号 (风格值, 1), 宽数组, 色值)

.子程序 表格边线, , , 输出表格横线（左端、交叉、右端、横线符号可变）
.参数 左端, 文本型
.参数 中间, 文本型
.参数 右端, 文本型
.参数 横线, 文本型
.参数 宽数组, 整数型, 数组
.参数 颜色, 整数型
.局部变量 j, 整数型
.局部变量 数量, 整数型
.局部变量 线, 文本型

数量 ＝ 取数组成员数 (宽数组)
线 ＝ 左端
.变量循环首 (1, 数量, 1, j)
    线 ＝ 线 ＋ 取重复文本 (宽数组[j] ＋ 2, 横线)
    .如果 (j ＜ 数量)
        线 ＝ 线 ＋ 中间
    .否则
        线 ＝ 线 ＋ 右端
    .如果结束

.变量循环尾 ()
设置控制台属性 (颜色)
写控制台 (线)
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)

.子程序 表格内容行, , , 输出表格一行内容（从数据数组的指定起点取 列数 个单元）
.参数 数据, 文本型, 数组
.参数 起点, 整数型
.参数 列数, 整数型
.参数 宽数组, 整数型, 数组
.参数 颜色, 整数型
.参数 竖线, 文本型
.参数 对齐数组, 整数型, 数组
.局部变量 j, 整数型
.局部变量 单元, 文本型
.局部变量 空格数, 整数型
.局部变量 前补, 整数型
.局部变量 对齐值, 整数型

设置控制台属性 (颜色)
写控制台 (竖线)
.变量循环首 (1, 列数, 1, j)
    .如果 (起点 ＋ j － 1 ≤ 取数组成员数 (数据))
        单元 ＝ 数据[起点 ＋ j － 1]
    .否则
        单元 ＝ “”
    .如果结束
    对齐值 ＝ 1
    .如果真 (j ≤ 取数组成员数 (对齐数组))
        对齐值 ＝ 对齐数组[j]
    .如果真结束
    空格数 ＝ 宽数组[j] － 显示宽度 (单元)
    .如果真 (空格数 ＜ 0)
        空格数 ＝ 0
    .如果真结束
    前补 ＝ 0
    .如果 (对齐值 ＝ 3)
        前补 ＝ 空格数
        空格数 ＝ 0
    .否则
        .如果 (对齐值 ＝ 2)
            前补 ＝ 取整 (空格数 ÷ 2)
            空格数 ＝ 空格数 － 前补
        .否则

        .如果结束

    .如果结束
    写控制台 (“ ” ＋ 取重复文本 (前补, “ ”) ＋ 单元 ＋ 取重复文本 (空格数, “ ”) ＋ “ ” ＋ 竖线)
.变量循环尾 ()
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)

.子程序 输出进度条, , , 输出进度条（配合 置光标位置 可在同一行刷新）
.参数 当前值, 整数型
.参数 总数, 整数型
.参数 宽度, 整数型, 可空
.参数 标题, 文本型, 可空
.参数 颜色, 整数型, 可空
.局部变量 条宽, 整数型
.局部变量 已完成, 整数型
.局部变量 百分比, 整数型
.局部变量 色值, 整数型

条宽 ＝ 30
.如果真 (是否为空 (宽度) ＝ 假 且 宽度 ＞ 0)
    条宽 ＝ 宽度
.如果真结束
色值 ＝ #控制台_亮绿
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
.如果真 (是否为空 (标题) ＝ 假 且 标题 ≠ “”)
    设置控制台属性 (#控制台_灰)
    写控制台 (标题 ＋ “ ”)
.如果真结束
.如果 (总数 ≤ 0)
    已完成 ＝ 条宽
    百分比 ＝ 100
.否则
    .如果 (当前值 ≤ 0)
        已完成 ＝ 0
        百分比 ＝ 0
    .否则
        已完成 ＝ 取整 (当前值 × 条宽 ÷ 总数)
        百分比 ＝ 取整 (当前值 × 100 ÷ 总数)
    .如果结束

.如果结束
.如果真 (已完成 ＞ 条宽)
    已完成 ＝ 条宽
.如果真结束
.如果真 (百分比 ＞ 100)
    百分比 ＝ 100
.如果真结束
设置控制台属性 (色值)
写控制台 (取重复文本 (已完成, “█”))
设置控制台属性 (#控制台_深灰)
写控制台 (取重复文本 (条宽 － 已完成, “?”))
设置控制台属性 (#控制台_灰)
写控制台 (“ ” ＋ 到文本 (百分比) ＋ “%”)

.子程序 取控制台宽度, 整数型, , 取控制台可见窗口宽度（列数）；失败时返回 80
.局部变量 信息, 控制台屏幕缓冲区信息

.如果真 (GetConsoleScreenBufferInfo (取输出句柄 (), 信息) ＝ 0)
    返回 (80)
.如果真结束
返回 (信息.窗口区域.右边 － 信息.窗口区域.左边 ＋ 1)

.子程序 取控制台高度, 整数型, , 取控制台可见窗口高度（行数）；失败时返回 25
.局部变量 信息, 控制台屏幕缓冲区信息

.如果真 (GetConsoleScreenBufferInfo (取输出句柄 (), 信息) ＝ 0)
    返回 (25)
.如果真结束
返回 (信息.窗口区域.下边 － 信息.窗口区域.上边 ＋ 1)

.子程序 取光标位置, , , 取当前光标坐标（从0开始）；失败时输出 0,0
.参数 横坐标, 整数型, 参考
.参数 纵坐标, 整数型, 参考
.局部变量 信息, 控制台屏幕缓冲区信息

横坐标 ＝ 0
纵坐标 ＝ 0
.如果真 (GetConsoleScreenBufferInfo (取输出句柄 (), 信息) ＝ 0)
    返回 ()
.如果真结束
横坐标 ＝ 信息.光标位置.X
纵坐标 ＝ 信息.光标位置.Y

.子程序 置光标位置, 逻辑型, , 移动光标到指定坐标（从0开始），成功返回真
.参数 横坐标, 整数型
.参数 纵坐标, 整数型

返回 (SetConsoleCursorPosition (取输出句柄 (), 横坐标 ＋ 纵坐标 × 65536) ≠ 0)

.子程序 取动画起始行, 整数型, , 动画行准备：先滚过帧行数预留底部留白，返回可原地重绘的起始行（自适应窗口高度）
.参数 帧行数, 整数型
.局部变量 列, 整数型
.局部变量 行, 整数型
.局部变量 i, 整数型

.计次循环首 (帧行数, i)
    写控制台 (#换行符)
.计次循环尾 ()
取光标位置 (列, 行)
返回 (行 － 帧行数)

.子程序 控制台可交互, 逻辑型, , 判断当前是否可尝试原地重绘（动画与方向键菜单的启用条件；定位不可用或强制静态时返回假）
.局部变量 列, 整数型
.局部变量 行, 整数型

.如果真 (g_动效模式 ＝ 1)
    返回 (假)
.如果真结束
.如果真 (g_动效模式 ＝ 2)
    返回 (真)
.如果真结束
取光标位置 (列, 行)
.如果真 (置光标位置 (0, 行) ＝ 假)
    返回 (假)
.如果真结束
返回 (真)

.子程序 显示宽度, 整数型, , 估算文本显示列宽（GBK 双字节字符按 2 列计）
.参数 文本, 文本型
.局部变量 数据, 字节集
.局部变量 总数, 整数型
.局部变量 i, 整数型
.局部变量 宽, 整数型

数据 ＝ 到字节集 (文本)
总数 ＝ 取字节集长度 (数据)
i ＝ 1
.判断循环首 (i ≤ 总数)
    .如果 (数据[i] ≥ 129 且 i ＜ 总数)
        宽 ＝ 宽 ＋ 2
        i ＝ i ＋ 2
    .否则
        宽 ＝ 宽 ＋ 1
        i ＝ i ＋ 1
    .如果结束

.判断循环尾 ()
返回 (宽)

.子程序 名字到颜色, 整数型, , 将颜色名映射为控制台颜色值（0-15），无法识别返回 -1
.参数 名称, 文本型

.如果真 (名称 ＝ “black”)
    返回 (0)
.如果真结束
.如果真 (名称 ＝ “maroon” 或 名称 ＝ “darkred” 或 名称 ＝ “dark_red”)
    返回 (4)
.如果真结束
.如果真 (名称 ＝ “green” 或 名称 ＝ “darkgreen” 或 名称 ＝ “dark_green”)
    返回 (2)
.如果真结束
.如果真 (名称 ＝ “olive” 或 名称 ＝ “darkyellow” 或 名称 ＝ “dark_yellow”)
    返回 (6)
.如果真结束
.如果真 (名称 ＝ “navy” 或 名称 ＝ “darkblue” 或 名称 ＝ “dark_blue”)
    返回 (1)
.如果真结束
.如果真 (名称 ＝ “purple” 或 名称 ＝ “darkmagenta” 或 名称 ＝ “dark_magenta”)
    返回 (5)
.如果真结束
.如果真 (名称 ＝ “teal” 或 名称 ＝ “darkcyan” 或 名称 ＝ “dark_cyan”)
    返回 (3)
.如果真结束
.如果真 (名称 ＝ “silver” 或 名称 ＝ “lightgray” 或 名称 ＝ “light_gray” 或 名称 ＝ “lightgrey”)
    返回 (7)
.如果真结束
.如果真 (名称 ＝ “gray” 或 名称 ＝ “grey” 或 名称 ＝ “darkgray” 或 名称 ＝ “dark_gray” 或 名称 ＝ “darkgrey” 或 名称 ＝ “bright_black”)
    返回 (8)
.如果真结束
.如果真 (名称 ＝ “red” 或 名称 ＝ “bright_red”)
    返回 (12)
.如果真结束
.如果真 (名称 ＝ “lime” 或 名称 ＝ “bright_green”)
    返回 (10)
.如果真结束
.如果真 (名称 ＝ “yellow” 或 名称 ＝ “bright_yellow”)
    返回 (14)
.如果真结束
.如果真 (名称 ＝ “blue” 或 名称 ＝ “bright_blue”)
    返回 (9)
.如果真结束
.如果真 (名称 ＝ “fuchsia” 或 名称 ＝ “magenta” 或 名称 ＝ “bright_magenta”)
    返回 (13)
.如果真结束
.如果真 (名称 ＝ “aqua” 或 名称 ＝ “cyan” 或 名称 ＝ “bright_cyan”)
    返回 (11)
.如果真结束
.如果真 (名称 ＝ “white” 或 名称 ＝ “bright_white”)
    返回 (15)
.如果真结束
.如果真 (名称 ＝ “default”)
    返回 (7)
.如果真结束
返回 (-1)

.子程序 十六进制色到属性, 整数型, , 将 #RRGGBB 近似映射为 16 色控制台属性，失败返回 -1
.参数 颜色文本, 文本型
.局部变量 内容, 文本型
.局部变量 红, 整数型
.局部变量 绿, 整数型
.局部变量 蓝, 整数型
.局部变量 结果, 整数型
.局部变量 最大值, 整数型
.局部变量 高亮, 整数型

内容 ＝ 颜色文本
.如果真 (取文本左边 (内容, 1) ＝ “#”)
    内容 ＝ 取文本中间 (内容, 2, 取文本长度 (内容) － 1)
.如果真结束
.如果真 (取文本长度 (内容) ≠ 6)
    返回 (-1)
.如果真结束
红 ＝ 十六进制字节 (取文本中间 (内容, 1, 2))
绿 ＝ 十六进制字节 (取文本中间 (内容, 3, 2))
蓝 ＝ 十六进制字节 (取文本中间 (内容, 5, 2))
.如果真 (红 ＜ 0 或 绿 ＜ 0 或 蓝 ＜ 0)
    返回 (-1)
.如果真结束
最大值 ＝ 红
.如果真 (绿 ＞ 最大值)
    最大值 ＝ 绿
.如果真结束
.如果真 (蓝 ＞ 最大值)
    最大值 ＝ 蓝
.如果真结束
高亮 ＝ 0
.如果真 (最大值 ＞ 160)
    高亮 ＝ 8
.如果真结束
.如果真 (取绝对值 (红 － 绿) ＜ 32 且 取绝对值 (绿 － 蓝) ＜ 32)
    .如果 (最大值 ≤ 64)
        返回 (0)
    .否则
        .如果 (最大值 ＞ 160)
            返回 (7)
        .否则
            返回 (8)
        .如果结束

    .如果结束

.如果真结束
结果 ＝ 0
.如果真 (红 ＞ 绿 且 红 ＞ 蓝 且 红 － 绿 ＞ 48 且 红 － 蓝 ＞ 48)
    结果 ＝ 4
.如果真结束
.如果真 (绿 ＞ 红 且 绿 ＞ 蓝 且 绿 － 红 ＞ 48 且 绿 － 蓝 ＞ 48)
    结果 ＝ 位或 (结果, 2)
.如果真结束
.如果真 (蓝 ＞ 红 且 蓝 ＞ 绿 且 蓝 － 红 ＞ 48 且 蓝 － 绿 ＞ 48)
    结果 ＝ 位或 (结果, 1)
.如果真结束
.如果真 (结果 ＝ 0)
    结果 ＝ 7
.如果真结束
返回 (位或 (结果, 高亮))

.子程序 十六进制色到分量, 逻辑型, , 将 #RRGGBB（可省略 #，大小写均可）解析为红绿蓝分量，成功返回真；失败返回假且不改变分量
.参数 颜色文本, 文本型
.参数 红, 整数型, 参考
.参数 绿, 整数型, 参考
.参数 蓝, 整数型, 参考
.局部变量 内容, 文本型
.局部变量 值红, 整数型
.局部变量 值绿, 整数型
.局部变量 值蓝, 整数型

内容 ＝ 颜色文本
.如果真 (取文本左边 (内容, 1) ＝ “#”)
    内容 ＝ 取文本中间 (内容, 2, 取文本长度 (内容) － 1)
.如果真结束
.如果真 (取文本长度 (内容) ≠ 6)
    返回 (假)
.如果真结束
值红 ＝ 十六进制字节 (取文本中间 (内容, 1, 2))
值绿 ＝ 十六进制字节 (取文本中间 (内容, 3, 2))
值蓝 ＝ 十六进制字节 (取文本中间 (内容, 5, 2))
.如果真 (值红 ＜ 0 或 值绿 ＜ 0 或 值蓝 ＜ 0)
    返回 (假)
.如果真结束
红 ＝ 值红
绿 ＝ 值绿
蓝 ＝ 值蓝
返回 (真)

.子程序 十六进制字节, 整数型, , 解析两个十六进制字符为 0-255，失败返回 -1
.参数 文本, 文本型
.局部变量 高, 整数型
.局部变量 低, 整数型

.如果真 (取文本长度 (文本) ≠ 2)
    返回 (-1)
.如果真结束
高 ＝ 十六进制位 (取代码 (文本, 1))
低 ＝ 十六进制位 (取代码 (文本, 2))
.如果真 (高 ＜ 0 或 低 ＜ 0)
    返回 (-1)
.如果真结束
返回 (高 × 16 ＋ 低)

.子程序 十六进制位, 整数型, , 将单个十六进制字符码转成 0-15，失败返回 -1
.参数 字符码, 整数型

.如果真 (字符码 ≥ 48 且 字符码 ≤ 57)
    返回 (字符码 － 48)
.如果真结束
.如果真 (字符码 ≥ 65 且 字符码 ≤ 70)
    返回 (字符码 － 55)
.如果真结束
.如果真 (字符码 ≥ 97 且 字符码 ≤ 102)
    返回 (字符码 － 87)
.如果真结束
返回 (-1)

.子程序 输出面板, , , 输出 Aurora 风格边框面板（内容多行用换行符分隔）
.参数 内容, 文本型
.参数 标题, 文本型, 可空
.参数 宽度, 整数型, 可空
.参数 颜色, 整数型, 可空
.参数 风格, 整数型, 可空
.局部变量 行数组, 文本型, , "0"
.局部变量 面板宽, 整数型
.局部变量 内宽, 整数型
.局部变量 色值, 整数型
.局部变量 风格值, 整数型
.局部变量 i, 整数型
.局部变量 单元, 文本型
.局部变量 空格数, 整数型
.局部变量 标题宽, 整数型

色值 ＝ #控制台_深灰
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
风格值 ＝ 1
.如果真 (是否为空 (风格) ＝ 假 且 风格 ≥ 1)
    风格值 ＝ 风格
.如果真结束
面板宽 ＝ 取控制台宽度 ()
.如果真 (是否为空 (宽度) ＝ 假 且 宽度 ≥ 8)
    面板宽 ＝ 宽度
.如果真结束
内宽 ＝ 面板宽 － 4
行数组 ＝ 分割文本 (内容, #换行符, )
设置控制台属性 (色值)
.如果 (是否为空 (标题) ＝ 假 且 标题 ≠ “” 且 显示宽度 (标题) ＋ 6 ≤ 面板宽)
    标题宽 ＝ 显示宽度 (标题)
    写控制台 (取边框符号 (风格值, 3) ＋ 取边框符号 (风格值, 1) ＋ “ ” ＋ 标题 ＋ “ ”)
    写控制台 (取重复文本 (面板宽 － 标题宽 － 5, 取边框符号 (风格值, 1)))
    写控制台 (取边框符号 (风格值, 5))
.否则
    写控制台 (取边框符号 (风格值, 3) ＋ 取重复文本 (面板宽 － 2, 取边框符号 (风格值, 1)) ＋ 取边框符号 (风格值, 5))
.如果结束
写控制台 (#换行符)
.变量循环首 (1, 取数组成员数 (行数组), 1, i)
    单元 ＝ 行数组[i]
    空格数 ＝ 内宽 － 显示宽度 (单元)
    .如果真 (空格数 ＜ 0)
        空格数 ＝ 0
    .如果真结束
    写控制台 (取边框符号 (风格值, 2) ＋ “ ” ＋ 单元 ＋ 取重复文本 (空格数, “ ”) ＋ “ ” ＋ 取边框符号 (风格值, 2))
    写控制台 (#换行符)
.变量循环尾 ()
写控制台 (取边框符号 (风格值, 9) ＋ 取重复文本 (面板宽 － 2, 取边框符号 (风格值, 1)) ＋ 取边框符号 (风格值, 11))
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)

.子程序 输出树, , , 输出 Aurora 风格树形结构（文本与层级并行数组）
.参数 根标题, 文本型
.参数 文本数组, 文本型, 数组
.参数 层级数组, 整数型, 数组
.参数 颜色, 整数型, 可空
.局部变量 数量, 整数型
.局部变量 i, 整数型
.局部变量 层, 整数型
.局部变量 前缀, 文本型
.局部变量 是否末子, 逻辑型
.局部变量 j, 整数型
.局部变量 祖先层, 整数型
.局部变量 祖先末, 逻辑型
.局部变量 找到祖先, 逻辑型
.局部变量 k, 整数型
.局部变量 色值, 整数型

数量 ＝ 取数组成员数 (文本数组)
.如果真 (取数组成员数 (层级数组) ＜ 数量)
    数量 ＝ 取数组成员数 (层级数组)
.如果真结束
色值 ＝ #控制台_深灰
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
设置控制台属性 (#控制台_白)
写控制台 (根标题)
写控制台 (#换行符)
.变量循环首 (1, 数量, 1, i)
    层 ＝ 层级数组[i]
    .如果真 (层 ＜ 1)
        层 ＝ 1
    .如果真结束
    是否末子 ＝ 真
    .变量循环首 (i ＋ 1, 数量, 1, j)
        .如果真 (层级数组[j] ＜ 层)
            跳出循环 ()
        .如果真结束
        .如果真 (层级数组[j] ＝ 层)
            是否末子 ＝ 假
            跳出循环 ()
        .如果真结束

    .变量循环尾 ()
    前缀 ＝ “”
    .变量循环首 (1, 层 － 1, 1, 祖先层)
        找到祖先 ＝ 假
        祖先末 ＝ 真
        .变量循环首 (i － 1, 1, -1, k)
            .如果真 (层级数组[k] ＝ 祖先层)
                找到祖先 ＝ 真
                跳出循环 ()
            .如果真结束

        .变量循环尾 ()
        .如果真 (找到祖先)
            .变量循环首 (k ＋ 1, 数量, 1, j)
                .如果真 (层级数组[j] ＜ 祖先层)
                    祖先末 ＝ 真
                    跳出循环 ()
                .如果真结束
                .如果真 (层级数组[j] ＝ 祖先层)
                    祖先末 ＝ 假
                    跳出循环 ()
                .如果真结束

            .变量循环尾 ()
        .如果真结束
        .如果 (祖先末)
            前缀 ＝ 前缀 ＋ “    ”
        .否则
            前缀 ＝ 前缀 ＋ “│   ”
        .如果结束

    .变量循环尾 ()
    设置控制台属性 (色值)
    .如果 (是否末子)
        写控制台 (前缀 ＋ “└── ”)
    .否则
        写控制台 (前缀 ＋ “├── ”)
    .如果结束
    设置控制台属性 (#控制台_灰)
    写控制台 (文本数组[i])
    写控制台 (#换行符)
.变量循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 输出柱状图, , , 输出 Aurora 风格水平柱状图
.参数 标签数组, 文本型, 数组
.参数 值数组, 整数型, 数组
.参数 最大值, 整数型, 可空
.参数 条宽, 整数型, 可空
.参数 颜色, 整数型, 可空
.局部变量 i, 整数型
.局部变量 数量, 整数型
.局部变量 最大标签宽, 整数型
.局部变量 顶值, 整数型
.局部变量 宽度值, 整数型
.局部变量 长度值, 整数型
.局部变量 色值, 整数型

数量 ＝ 取数组成员数 (标签数组)
.如果真 (取数组成员数 (值数组) ＜ 数量)
    数量 ＝ 取数组成员数 (值数组)
.如果真结束
顶值 ＝ 0
.如果真 (是否为空 (最大值) ＝ 假 且 最大值 ＞ 0)
    顶值 ＝ 最大值
.如果真结束
.变量循环首 (1, 数量, 1, i)
    .如果真 (值数组[i] ＞ 顶值)
        顶值 ＝ 值数组[i]
    .如果真结束

.变量循环尾 ()
.如果真 (顶值 ≤ 0)
    顶值 ＝ 1
.如果真结束
宽度值 ＝ 40
.如果真 (是否为空 (条宽) ＝ 假 且 条宽 ＞ 0)
    宽度值 ＝ 条宽
.如果真结束
色值 ＝ #控制台_亮青
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
最大标签宽 ＝ 0
.变量循环首 (1, 数量, 1, i)
    .如果真 (显示宽度 (标签数组[i]) ＞ 最大标签宽)
        最大标签宽 ＝ 显示宽度 (标签数组[i])
    .如果真结束

.变量循环尾 ()
.变量循环首 (1, 数量, 1, i)
    设置控制台属性 (#控制台_灰)
    写控制台 (标签数组[i] ＋ 取重复文本 (最大标签宽 － 显示宽度 (标签数组[i]), “ ”) ＋ “ │”)
    长度值 ＝ 取整 (值数组[i] × 宽度值 ÷ 顶值)
    .如果真 (长度值 ＜ 1 且 值数组[i] ＞ 0)
        长度值 ＝ 1
    .如果真结束
    设置控制台属性 (色值)
    写控制台 (取重复文本 (长度值, “█”))
    设置控制台属性 (#控制台_深灰)
    写控制台 (“ ” ＋ 到文本 (值数组[i]))
    写控制台 (#换行符)
.变量循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 输出加载动画, , , 在同一行播放旋转指示器动画（非控制台环境降级为静态输出）
.参数 文本, 文本型
.参数 帧数, 整数型, 可空
.参数 间隔毫秒, 整数型, 可空
.局部变量 帧, 文本型, , "0"
.局部变量 总帧, 整数型
.局部变量 间隔, 整数型
.局部变量 i, 整数型
.局部变量 帧索引, 整数型
.局部变量 列, 整数型
.局部变量 行, 整数型
.局部变量 可定位, 逻辑型

清除数组 (帧)
加入成员 (帧, “|”)
加入成员 (帧, “/”)
加入成员 (帧, “-”)
加入成员 (帧, “\”)
总帧 ＝ 8
.如果真 (是否为空 (帧数) ＝ 假 且 帧数 ＞ 0)
    总帧 ＝ 帧数
.如果真结束
间隔 ＝ 60
.如果真 (是否为空 (间隔毫秒) ＝ 假 且 间隔毫秒 ＞ 0)
    间隔 ＝ 间隔毫秒
.如果真结束
取光标位置 (列, 行)
可定位 ＝ 控制台可交互 ()
.如果 (可定位)
    行 ＝ 取动画起始行 (1)
    帧索引 ＝ 0
    .计次循环首 (总帧, i)
        帧索引 ＝ 帧索引 ＋ 1
        .如果真 (帧索引 ＞ 4)
            帧索引 ＝ 1
        .如果真结束
        置光标位置 (0, 行)
        设置控制台属性 (#控制台_亮青)
        写控制台 (帧[帧索引])
        设置控制台属性 (#控制台_灰)
        写控制台 (“ ” ＋ 文本 ＋ “ ...  ”)
        延时 (间隔)
    .计次循环尾 ()
    置光标位置 (0, 行)
    设置控制台属性 (#控制台_亮绿)
    写控制台 (“√”)
    设置控制台属性 (#控制台_灰)
    写控制台 (“ ” ＋ 文本 ＋ “ 完成！”)
    写控制台 (#换行符)
.否则
    设置控制台属性 (#控制台_灰)
    写控制台 (文本 ＋ “ ...”)
    写控制台 (#换行符)
.如果结束


.子程序 读取输入行, 文本型, , 从标准输入读取一行文本；失败或无输入时返回空文本
.局部变量 缓冲, 文本型
.局部变量 读取数, 整数型
.局部变量 结果, 文本型
.局部变量 i, 整数型

缓冲 ＝ 取空白文本 (255)
.如果真 (ReadConsoleA (GetStdHandle (#控制台_标准输入), 缓冲, 255, 读取数, 0) ＝ 0)
    返回 (“”)
.如果真结束
.如果真 (读取数 ≤ 0)
    返回 (“”)
.如果真结束
结果 ＝ 缓冲
i ＝ 1
.判断循环首 (i ≤ 取文本长度 (结果))
    .如果真 (取代码 (结果, i) ＜ 32)
        结果 ＝ 取文本左边 (结果, i － 1)
        跳出循环 ()
    .如果真结束
    i ＝ i ＋ 1
.判断循环尾 ()
返回 (结果)

.子程序 提示输入文本, 文本型, , Aurora 风格文本输入提示，回车使用默认值
.参数 提示语, 文本型
.参数 默认值, 文本型, 可空
.局部变量 输入, 文本型

设置控制台属性 (#控制台_亮青)
写控制台 (“? ”)
设置控制台属性 (#控制台_白)
写控制台 (提示语)
设置控制台属性 (#控制台_深灰)
.如果真 (是否为空 (默认值) ＝ 假 且 默认值 ≠ “”)
    写控制台 (“ [” ＋ 默认值 ＋ “]”)
.如果真结束
写控制台 (“: ”)
设置控制台属性 (#控制台_灰)
输入 ＝ 读取输入行 ()
.如果真 (输入 ＝ “”)
    输入 ＝ 默认值
.如果真结束
返回 (输入)

.子程序 提示确认, 逻辑型, , Aurora 风格确认提示，输入 y/n，回车使用默认值
.参数 提示语, 文本型
.参数 默认值, 逻辑型, 可空
.局部变量 输入, 文本型
.局部变量 默认真, 逻辑型

默认真 ＝ 真
.如果真 (是否为空 (默认值) ＝ 假)
    默认真 ＝ 默认值
.如果真结束
设置控制台属性 (#控制台_亮青)
写控制台 (“? ”)
设置控制台属性 (#控制台_白)
写控制台 (提示语)
设置控制台属性 (#控制台_深灰)
.如果 (默认真)
    写控制台 (“ [Y/n]: ”)
.否则
    写控制台 (“ [y/N]: ”)
.如果结束
设置控制台属性 (#控制台_灰)
输入 ＝ 到小写 (读取输入行 ())
.如果真 (输入 ＝ “”)
    返回 (默认真)
.如果真结束
.如果真 (输入 ＝ “y” 或 输入 ＝ “yes”)
    返回 (真)
.如果真结束
返回 (假)

.子程序 提示选择, 整数型, , Aurora 风格单选提示，输入序号并返回（1 起）；无效输入返回 0
.参数 提示语, 文本型
.参数 选项数组, 文本型, 数组
.局部变量 i, 整数型
.局部变量 数量, 整数型
.局部变量 输入, 文本型
.局部变量 序号, 整数型

数量 ＝ 取数组成员数 (选项数组)
.如果真 (数量 ≤ 0)
    返回 (0)
.如果真结束
设置控制台属性 (#控制台_亮青)
写控制台 (“? ”)
设置控制台属性 (#控制台_白)
写控制台 (提示语)
写控制台 (#换行符)
.变量循环首 (1, 数量, 1, i)
    设置控制台属性 (#控制台_深灰)
    写控制台 (“  ” ＋ 到文本 (i) ＋ “. ”)
    设置控制台属性 (#控制台_灰)
    写控制台 (选项数组[i])
    写控制台 (#换行符)
.变量循环尾 ()
设置控制台属性 (#控制台_亮青)
写控制台 (“  请输入序号: ”)
设置控制台属性 (#控制台_灰)
输入 ＝ 读取输入行 ()
序号 ＝ 取整 (到数值 (输入))
.如果 (序号 ≥ 1 且 序号 ≤ 数量)
    返回 (序号)
.否则
    返回 (0)
.如果结束


.子程序 输出列布局, , , 输出多列对齐布局（按列优先填充），默认 3 列
.参数 项目数组, 文本型, 数组
.参数 列数, 整数型, 可空
.参数 间距, 整数型, 可空
.参数 颜色, 整数型, 可空
.局部变量 数量, 整数型
.局部变量 列宽, 整数型, , "0"
.局部变量 行数, 整数型
.局部变量 列号, 整数型
.局部变量 行号, 整数型
.局部变量 序号, 整数型
.局部变量 线, 文本型
.局部变量 宽值, 整数型
.局部变量 间距值, 整数型
.局部变量 色值, 整数型
.局部变量 有效列数, 整数型
.局部变量 项目, 文本型

数量 ＝ 取数组成员数 (项目数组)
.如果真 (数量 ≤ 0)
    返回 ()
.如果真结束
间距值 ＝ 4
.如果真 (是否为空 (间距) ＝ 假 且 间距 ≥ 1)
    间距值 ＝ 间距
.如果真结束
色值 ＝ #控制台_灰
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
.如果 (是否为空 (列数) 或 列数 ≤ 0)
    有效列数 ＝ 3
.否则
    有效列数 ＝ 列数
.如果结束
.如果真 (有效列数 ＞ 数量)
    有效列数 ＝ 数量
.如果真结束
行数 ＝ 取整 ((数量 ＋ 有效列数 － 1) ÷ 有效列数)
重定义数组 (列宽, 假, 有效列数)
.变量循环首 (1, 有效列数, 1, 列号)
    宽值 ＝ 0
    .变量循环首 (1, 行数, 1, 行号)
        序号 ＝ (列号 － 1) × 行数 ＋ 行号
        .如果真 (序号 ≤ 数量)
            .如果真 (显示宽度 (项目数组[序号]) ＞ 宽值)
                宽值 ＝ 显示宽度 (项目数组[序号])
            .如果真结束

        .如果真结束

    .变量循环尾 ()
    列宽[列号] ＝ 宽值
.变量循环尾 ()
设置控制台属性 (色值)
.变量循环首 (1, 行数, 1, 行号)
    线 ＝ “”
    .变量循环首 (1, 有效列数, 1, 列号)
        序号 ＝ (列号 － 1) × 行数 ＋ 行号
        .如果真 (序号 ≤ 数量)
            项目 ＝ 项目数组[序号]
            线 ＝ 线 ＋ 项目 ＋ 取重复文本 (列宽[列号] － 显示宽度 (项目) ＋ 间距值, “ ”)
        .如果真结束

    .变量循环尾 ()
    写控制台 (线)
    写控制台 (#换行符)
.变量循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 输出对齐行, , , 输出一行文本并按指定方式对齐（1=左 2=中 3=右）
.参数 文本, 文本型
.参数 宽度, 整数型
.参数 对齐, 整数型
.参数 颜色, 整数型
.局部变量 行宽, 整数型
.局部变量 空格数, 整数型

行宽 ＝ 显示宽度 (文本)
.如果 (对齐 ＝ 2)
    空格数 ＝ 取整 ((宽度 － 行宽) ÷ 2)
.否则
    .如果 (对齐 ＝ 3)
        空格数 ＝ 宽度 － 行宽
    .否则
        空格数 ＝ 0
    .如果结束

.如果结束
.如果真 (空格数 ＜ 0)
    空格数 ＝ 0
.如果真结束
设置控制台属性 (颜色)
.如果真 (空格数 ＞ 0)
    写控制台 (取重复文本 (空格数, “ ”))
.如果真结束
写控制台 (文本)
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)

.子程序 输出段落, , , 输出自动换行段落并按对齐方式显示（1=左 2=中 3=右）
.参数 文本, 文本型
.参数 宽度, 整数型, 可空
.参数 对齐, 整数型, 可空
.参数 颜色, 整数型, 可空
.局部变量 行数组, 文本型, , "0"
.局部变量 段宽, 整数型
.局部变量 对齐值, 整数型
.局部变量 色值, 整数型
.局部变量 i, 整数型
.局部变量 行宽, 整数型
.局部变量 缓冲, 文本型
.局部变量 数据, 字节集
.局部变量 指针, 整数型
.局部变量 总数, 整数型
.局部变量 字节, 整数型
.局部变量 字符宽, 整数型
.局部变量 字符文本, 文本型
.局部变量 空格位, 整数型

段宽 ＝ 取控制台宽度 ()
.如果真 (是否为空 (宽度) ＝ 假 且 宽度 ≥ 8)
    段宽 ＝ 宽度
.如果真结束
对齐值 ＝ 1
.如果真 (是否为空 (对齐) ＝ 假)
    对齐值 ＝ 对齐
.如果真结束
色值 ＝ #控制台_灰
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
行数组 ＝ 分割文本 (文本, #换行符, )
.变量循环首 (1, 取数组成员数 (行数组), 1, i)
    数据 ＝ 到字节集 (行数组[i])
    总数 ＝ 取字节集长度 (数据)
    缓冲 ＝ “”
    行宽 ＝ 0
    指针 ＝ 1
    .判断循环首 (指针 ≤ 总数)
        字节 ＝ 数据[指针]
        .如果 (字节 ≥ 129 且 指针 ＜ 总数)
            字符宽 ＝ 2
            字符文本 ＝ 取文本中间 (行数组[i], 指针, 2)
            指针 ＝ 指针 ＋ 2
        .否则
            字符宽 ＝ 1
            字符文本 ＝ 取文本中间 (行数组[i], 指针, 1)
            指针 ＝ 指针 ＋ 1
        .如果结束
        .如果真 (行宽 ＋ 字符宽 ＞ 段宽 且 行宽 ＞ 0)
            空格位 ＝ 倒找文本 (缓冲, “ ”, , 假)
            .如果 (空格位 ＞ 1 且 空格位 ＜ 取文本长度 (缓冲))
                .如果 (显示宽度 (取文本中间 (缓冲, 空格位 ＋ 1, 取文本长度 (缓冲) － 空格位)) ＋ 字符宽 ≤ 段宽)
                    输出对齐行 (取文本左边 (缓冲, 空格位 － 1), 段宽, 对齐值, 色值)
                    缓冲 ＝ 取文本中间 (缓冲, 空格位 ＋ 1, 取文本长度 (缓冲) － 空格位)
                    行宽 ＝ 显示宽度 (缓冲)
                .否则
                    输出对齐行 (缓冲, 段宽, 对齐值, 色值)
                    缓冲 ＝ “”
                    行宽 ＝ 0
                .如果结束

            .否则
                输出对齐行 (缓冲, 段宽, 对齐值, 色值)
                缓冲 ＝ “”
                行宽 ＝ 0
            .如果结束

        .如果真结束
        缓冲 ＝ 缓冲 ＋ 字符文本
        行宽 ＝ 行宽 ＋ 字符宽
    .判断循环尾 ()
    输出对齐行 (缓冲, 段宽, 对齐值, 色值)
.变量循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 取边框符号, 文本型, , 取指定边框风格的第 N 个符号（1横线 2竖线 3左上 4中上 5右上 6左中 7中心 8右中 9左下 10中下 11右下）
.参数 风格, 整数型
.参数 序号, 整数型
.局部变量 符号组, 文本型, , "0"

符号组 ＝ 分割文本 (边框组 (风格), “,”, )
.如果 (序号 ≥ 1 且 序号 ≤ 取数组成员数 (符号组))
    返回 (符号组[序号])
.否则
    返回 (“ ”)
.如果结束


.子程序 边框组, 文本型, , 返回边框风格对应的符号串
.参数 风格, 整数型

.如果 (风格 ＝ 2)
    返回 (“━,┃,┏,┳,┓,┣,╋,┫,┗,┻,┛”)
.否则
    .如果 (风格 ＝ 3)
        返回 (“═,║,╔,╦,╗,╠,╬,╣,╚,╩,╝”)
    .否则
        .如果 (风格 ＝ 4)
            返回 (“─,│,╭,┬,╮,├,┼,┤,╰,┴,╯”)
        .否则
            .如果 (风格 ＝ 5)
                返回 (“-,|,+,+,+,+,+,+,+,+,+”)
            .否则
                返回 (“─,│,┌,┬,┐,├,┼,┤,└,┴,┘”)
            .如果结束

        .如果结束

    .如果结束

.如果结束


.子程序 选择菜单, 整数型, , 方向键选择菜单（↑↓ 移动，回车确认，Esc 取消）；无法读取控制台事件时回退为序号输入
.参数 提示语, 文本型
.参数 选项数组, 文本型, 数组
.局部变量 数量, 整数型
.局部变量 选中, 整数型
.局部变量 记录, 输入记录
.局部变量 读取数, 整数型
.局部变量 键码, 整数型
.局部变量 列, 整数型
.局部变量 行, 整数型
.局部变量 完成, 逻辑型

数量 ＝ 取数组成员数 (选项数组)
.如果真 (数量 ≤ 0)
    返回 (0)
.如果真结束
设置控制台属性 (#控制台_亮青)
写控制台 (“? ”)
设置控制台属性 (#控制台_白)
写控制台 (提示语)
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)
取光标位置 (列, 行)
选中 ＝ 1
.如果真 (控制台可交互 () ＝ 假)
    输出选项清单 (选项数组)
    返回 (回退选单输入 (数量))
.如果真结束
行 ＝ 取动画起始行 (数量)
.如果真 (重绘菜单 (行, 选中, 数量, 选项数组) ＝ 假)
    输出选项清单 (选项数组)
    返回 (回退选单输入 (数量))
.如果真结束
FlushConsoleInputBuffer (GetStdHandle (#控制台_标准输入))
完成 ＝ 假
.判断循环首 (完成 ＝ 假)
    .如果真 (ReadConsoleInputA (GetStdHandle (#控制台_标准输入), 记录, 1, 读取数) ＝ 0 或 读取数 ＜ 1)
        返回 (回退选单输入 (数量))
    .如果真结束
    .如果真 (记录.事件类型 ＝ 1 且 记录.按键按下 ≠ 0)
        键码 ＝ 记录.虚拟键码
        .判断开始 (键码 ＝ 38)
            .如果真 (选中 ＞ 1)
                选中 ＝ 选中 － 1
                .如果真 (重绘菜单 (行, 选中, 数量, 选项数组) ＝ 假)
                    返回 (回退选单输入 (数量))
                .如果真结束

            .如果真结束

        .判断 (键码 ＝ 40)
            .如果真 (选中 ＜ 数量)
                选中 ＝ 选中 ＋ 1
                .如果真 (重绘菜单 (行, 选中, 数量, 选项数组) ＝ 假)
                    返回 (回退选单输入 (数量))
                .如果真结束

            .如果真结束

        .判断 (键码 ＝ 13)
            完成 ＝ 真
        .判断 (键码 ＝ 27)
            选中 ＝ 0
            完成 ＝ 真
        .默认

        .判断结束

    .如果真结束

.判断循环尾 ()
FlushConsoleInputBuffer (GetStdHandle (#控制台_标准输入))
置光标位置 (0, 行 ＋ 数量)
返回 (选中)

.子程序 重绘菜单, 逻辑型, , 重绘菜单列表（方向键移动时局部刷新）；无法定位光标时返回假
.参数 起始行, 整数型
.参数 选中, 整数型
.参数 数量, 整数型
.参数 选项数组, 文本型, 数组
.局部变量 i, 整数型

.如果真 (置光标位置 (0, 起始行) ＝ 假)
    返回 (假)
.如果真结束
.计次循环首 (数量, i)
    .如果 (i ＝ 选中)
        设置控制台属性 (#控制台_亮青)
        写控制台 (“> ” ＋ 选项数组[i])
    .否则
        设置控制台属性 (#控制台_灰)
        写控制台 (“  ” ＋ 选项数组[i])
    .如果结束
    写控制台 (取重复文本 (16, “ ”))
    写控制台 (#换行符)
.计次循环尾 ()
设置控制台属性 (#控制台_灰)
返回 (真)

.子程序 回退选单输入, 整数型, , 控制台事件不可用时的序号输入回退实现
.参数 数量, 整数型
.局部变量 输入, 文本型
.局部变量 序号, 整数型

设置控制台属性 (#控制台_亮青)
写控制台 (“  请输入序号: ”)
设置控制台属性 (#控制台_灰)
输入 ＝ 读取输入行 ()
序号 ＝ 取整 (到数值 (输入))
.如果 (序号 ≥ 1 且 序号 ≤ 数量)
    返回 (序号)
.否则
    返回 (0)
.如果结束


.子程序 输出选项清单, , , 无法定位重绘时以纯文本序号列表输出候选项
.参数 选项数组, 文本型, 数组
.局部变量 i, 整数型

.计次循环首 (取数组成员数 (选项数组), i)
    设置控制台属性 (#控制台_深灰)
    写控制台 (“  ” ＋ 到文本 (i) ＋ “. ”)
    设置控制台属性 (#控制台_灰)
    写控制台 (选项数组[i])
    写控制台 (#换行符)
.计次循环尾 ()

.子程序 回退多选输入, , , 控制台事件不可用时的多选序号回退实现（逗号分隔）
.参数 数量, 整数型
.参数 选中数组, 整数型, 参考 数组
.局部变量 输入, 文本型
.局部变量 片段, 文本型, , "0"
.局部变量 i, 整数型
.局部变量 序号, 整数型

设置控制台属性 (#控制台_亮青)
写控制台 (“  请输入序号（逗号分隔）: ”)
设置控制台属性 (#控制台_灰)
输入 ＝ 读取输入行 ()
写控制台 (#换行符)
片段 ＝ 分割文本 (输入, “,”, )
.计次循环首 (取数组成员数 (片段), i)
    序号 ＝ 取整 (到数值 (片段[i]))
    .如果真 (序号 ≥ 1 且 序号 ≤ 数量)
        加入成员 (选中数组, 序号)
    .如果真结束

.计次循环尾 ()

.子程序 启用虚拟终端, 逻辑型, , 尝试启用控制台虚拟终端处理（真彩色与更多样式）
.局部变量 句柄, 整数型
.局部变量 模式, 整数型

g_VT支持 ＝ 假
句柄 ＝ 取输出句柄 ()
.如果真 (GetConsoleMode (句柄, 模式) ＝ 0)
    返回 (假)
.如果真结束
.如果真 (SetConsoleMode (句柄, 位或 (模式, 4)) ＝ 0)
    返回 (假)
.如果真结束
g_VT支持 ＝ 真
返回 (真)

.子程序 输出真彩行, , , 输出一行真彩色文本；颜色文本形如 #FF8800（可省略 #，大小写均可），解析失败回退灰色；不支持虚拟终端时回退为近似 16 色
.参数 文本, 文本型
.参数 颜色文本, 文本型
.局部变量 红, 整数型
.局部变量 绿, 整数型
.局部变量 蓝, 整数型

.如果真 (十六进制色到分量 (颜色文本, 红, 绿, 蓝) ＝ 假)
    红 ＝ 128
    绿 ＝ 128
    蓝 ＝ 128
.如果真结束
.如果 (g_VT支持)
    写控制台 (字符 (27) ＋ “[38;2;” ＋ 到文本 (红) ＋ “;” ＋ 到文本 (绿) ＋ “;” ＋ 到文本 (蓝) ＋ “m” ＋ 文本 ＋ 字符 (27) ＋ “[0m”)
    写控制台 (#换行符)
.否则
    设置控制台属性 (RGB到属性 (红, 绿, 蓝))
    写控制台 (文本)
    写控制台 (#换行符)
    设置控制台属性 (#控制台_灰)
.如果结束


.子程序 RGB到属性, 整数型, , 将 RGB 分量近似映射为 16 色控制台属性
.参数 红, 整数型
.参数 绿, 整数型
.参数 蓝, 整数型
.局部变量 结果, 整数型
.局部变量 最大值, 整数型
.局部变量 高亮, 整数型

最大值 ＝ 红
.如果真 (绿 ＞ 最大值)
    最大值 ＝ 绿
.如果真结束
.如果真 (蓝 ＞ 最大值)
    最大值 ＝ 蓝
.如果真结束
高亮 ＝ 0
.如果真 (最大值 ＞ 160)
    高亮 ＝ 8
.如果真结束
.如果真 (取绝对值 (红 － 绿) ＜ 32 且 取绝对值 (绿 － 蓝) ＜ 32)
    .如果 (最大值 ≤ 64)
        返回 (0)
    .否则
        .如果 (最大值 ＞ 160)
            返回 (7)
        .否则
            返回 (8)
        .如果结束

    .如果结束

.如果真结束
结果 ＝ 0
.如果真 (红 ＞ 绿 且 红 ＞ 蓝 且 红 － 绿 ＞ 48 且 红 － 蓝 ＞ 48)
    结果 ＝ 4
.如果真结束
.如果真 (绿 ＞ 红 且 绿 ＞ 蓝 且 绿 － 红 ＞ 48 且 绿 － 蓝 ＞ 48)
    结果 ＝ 位或 (结果, 2)
.如果真结束
.如果真 (蓝 ＞ 红 且 蓝 ＞ 绿 且 蓝 － 红 ＞ 48 且 蓝 － 绿 ＞ 48)
    结果 ＝ 位或 (结果, 1)
.如果真结束
.如果真 (结果 ＝ 0)
    结果 ＝ 7
.如果真结束
返回 (位或 (结果, 高亮))

.子程序 输出占比图, , , 输出占比堆叠图（横向色块 ＋ 可选图例百分比）
.参数 标签数组, 文本型, 数组
.参数 值数组, 整数型, 数组
.参数 条宽, 整数型, 可空
.参数 显示图例, 逻辑型, 可空
.局部变量 数量, 整数型
.局部变量 总数, 整数型
.局部变量 宽度值, 整数型
.局部变量 调色板, 整数型, , "0"
.局部变量 已用, 整数型
.局部变量 累计, 整数型
.局部变量 i, 整数型
.局部变量 段长, 整数型
.局部变量 千分比, 整数型
.局部变量 整数部分, 整数型
.局部变量 小数部分, 整数型
.局部变量 调色索引, 整数型

数量 ＝ 取数组成员数 (标签数组)
.如果真 (取数组成员数 (值数组) ＜ 数量)
    数量 ＝ 取数组成员数 (值数组)
.如果真结束
.如果真 (数量 ≤ 0)
    返回 ()
.如果真结束
宽度值 ＝ 40
.如果真 (是否为空 (条宽) ＝ 假 且 条宽 ＞ 0)
    宽度值 ＝ 条宽
.如果真结束
总数 ＝ 0
.变量循环首 (1, 数量, 1, i)
    总数 ＝ 总数 ＋ 值数组[i]
.变量循环尾 ()
.如果真 (总数 ≤ 0)
    返回 ()
.如果真结束
清除数组 (调色板)
加入成员 (调色板, #控制台_亮红)
加入成员 (调色板, #控制台_亮绿)
加入成员 (调色板, #控制台_亮黄)
加入成员 (调色板, #控制台_亮蓝)
加入成员 (调色板, #控制台_亮紫)
加入成员 (调色板, #控制台_亮青)
已用 ＝ 0
累计 ＝ 0
.变量循环首 (1, 数量, 1, i)
    累计 ＝ 累计 ＋ 值数组[i]
    段长 ＝ 取整 (累计 × 宽度值 ÷ 总数) － 已用
    .如果真 (段长 ＜ 0)
        段长 ＝ 0
    .如果真结束
    调色索引 ＝ i － 取整 ((i － 1) ÷ 6) × 6
    设置控制台属性 (调色板[调色索引])
    写控制台 (取重复文本 (段长, “█”))
    已用 ＝ 已用 ＋ 段长
.变量循环尾 ()
设置控制台属性 (#控制台_灰)
写控制台 (“  总计 ” ＋ 到文本 (总数))
写控制台 (#换行符)
.如果真 (是否为空 (显示图例) ＝ 假 且 显示图例)
    .变量循环首 (1, 数量, 1, i)
        调色索引 ＝ i － 取整 ((i － 1) ÷ 6) × 6
        千分比 ＝ 取整 (值数组[i] × 1000 ÷ 总数)
        整数部分 ＝ 取整 (千分比 ÷ 10)
        小数部分 ＝ 千分比 － 整数部分 × 10
        设置控制台属性 (调色板[调色索引])
        写控制台 (“■ ”)
        设置控制台属性 (#控制台_灰)
        写控制台 (标签数组[i] ＋ “ ” ＋ 到文本 (整数部分) ＋ “.” ＋ 到文本 (小数部分) ＋ “%”)
        .如果真 (i ＜ 数量)
            写控制台 (“    ”)
        .如果真结束

    .变量循环尾 ()
    写控制台 (#换行符)
.如果真结束
设置控制台属性 (#控制台_灰)

.子程序 输出进度组, , , 输出多任务进度块（每任务一行，配合 置光标位置 可整块刷新）
.参数 名称数组, 文本型, 数组
.参数 值数组, 整数型, 数组
.参数 总数, 整数型
.参数 条宽, 整数型, 可空
.局部变量 数量, 整数型
.局部变量 宽度值, 整数型
.局部变量 名称宽, 整数型
.局部变量 i, 整数型
.局部变量 已完成, 整数型
.局部变量 百分比, 整数型
.局部变量 色数组, 整数型, , "0"

数量 ＝ 取数组成员数 (名称数组)
.如果真 (取数组成员数 (值数组) ＜ 数量)
    数量 ＝ 取数组成员数 (值数组)
.如果真结束
.如果真 (数量 ≤ 0)
    返回 ()
.如果真结束
.如果真 (总数 ≤ 0)
    总数 ＝ 100
.如果真结束
宽度值 ＝ 24
.如果真 (是否为空 (条宽) ＝ 假 且 条宽 ＞ 0)
    宽度值 ＝ 条宽
.如果真结束
名称宽 ＝ 0
.变量循环首 (1, 数量, 1, i)
    .如果真 (显示宽度 (名称数组[i]) ＞ 名称宽)
        名称宽 ＝ 显示宽度 (名称数组[i])
    .如果真结束

.变量循环尾 ()
清除数组 (色数组)
加入成员 (色数组, #控制台_亮青)
加入成员 (色数组, #控制台_亮绿)
加入成员 (色数组, #控制台_亮黄)
加入成员 (色数组, #控制台_亮紫)
加入成员 (色数组, #控制台_亮蓝)
.变量循环首 (1, 数量, 1, i)
    写控制台 (“  ”)
    设置控制台属性 (#控制台_白)
    写控制台 (名称数组[i])
    写控制台 (取重复文本 (名称宽 － 显示宽度 (名称数组[i]) ＋ 1, “ ”))
    已完成 ＝ 取整 (值数组[i] × 宽度值 ÷ 总数)
    .如果真 (已完成 ＜ 0)
        已完成 ＝ 0
    .如果真结束
    .如果真 (已完成 ＞ 宽度值)
        已完成 ＝ 宽度值
    .如果真结束
    设置控制台属性 (色数组[i － 取整 ((i － 1) ÷ 5) × 5])
    写控制台 (取重复文本 (已完成, “━”))
    设置控制台属性 (#控制台_深灰)
    写控制台 (取重复文本 (宽度值 － 已完成, “─”))
    百分比 ＝ 取整 (值数组[i] × 100 ÷ 总数)
    .如果真 (百分比 ＜ 0)
        百分比 ＝ 0
    .如果真结束
    .如果真 (百分比 ＞ 100)
        百分比 ＝ 100
    .如果真结束
    设置控制台属性 (#控制台_灰)
    写控制台 (“ ” ＋ 到文本 (百分比) ＋ “%”)
    写控制台 (#换行符)
.变量循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 重绘多选, 逻辑型, , 重绘多选菜单列表（勾选状态与光标行）；无法定位光标时返回假
.参数 起始行, 整数型
.参数 光标, 整数型
.参数 勾选, 逻辑型, 数组
.参数 数量, 整数型
.参数 选项数组, 文本型, 数组
.局部变量 i, 整数型
.局部变量 前缀, 文本型

.如果真 (置光标位置 (0, 起始行) ＝ 假)
    返回 (假)
.如果真结束
.计次循环首 (数量, i)
    .如果 (勾选[i])
        前缀 ＝ “[x] ”
    .否则
        前缀 ＝ “[ ] ”
    .如果结束
    .如果 (i ＝ 光标)
        设置控制台属性 (#控制台_亮青)
        写控制台 (“> ” ＋ 前缀 ＋ 选项数组[i])
    .否则
        设置控制台属性 (#控制台_灰)
        写控制台 (“  ” ＋ 前缀 ＋ 选项数组[i])
    .如果结束
    写控制台 (取重复文本 (12, “ ”))
    写控制台 (#换行符)
.计次循环尾 ()
设置控制台属性 (#控制台_灰)
返回 (真)

.子程序 多选菜单, , , 方向键多选菜单（空格勾选，回车确认，Esc 取消）；结果写入选中数组
.参数 提示语, 文本型
.参数 选项数组, 文本型, 数组
.参数 选中数组, 整数型, 参考 数组
.局部变量 数量, 整数型
.局部变量 光标, 整数型
.局部变量 勾选, 逻辑型, , "0"
.局部变量 记录, 输入记录
.局部变量 读取数, 整数型
.局部变量 键码, 整数型
.局部变量 列, 整数型
.局部变量 行, 整数型
.局部变量 完成, 逻辑型
.局部变量 i, 整数型

清除数组 (选中数组)
数量 ＝ 取数组成员数 (选项数组)
.如果真 (数量 ≤ 0)
    返回 ()
.如果真结束
重定义数组 (勾选, 假, 数量)
设置控制台属性 (#控制台_亮青)
写控制台 (“? ”)
设置控制台属性 (#控制台_白)
写控制台 (提示语)
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)
取光标位置 (列, 行)
光标 ＝ 1
.如果真 (控制台可交互 () ＝ 假)
    输出选项清单 (选项数组)
    回退多选输入 (数量, 选中数组)
    返回 ()
.如果真结束
行 ＝ 取动画起始行 (数量)
.如果真 (重绘多选 (行, 光标, 勾选, 数量, 选项数组) ＝ 假)
    输出选项清单 (选项数组)
    回退多选输入 (数量, 选中数组)
    返回 ()
.如果真结束
FlushConsoleInputBuffer (GetStdHandle (#控制台_标准输入))
完成 ＝ 假
.判断循环首 (完成 ＝ 假)
    .如果真 (ReadConsoleInputA (GetStdHandle (#控制台_标准输入), 记录, 1, 读取数) ＝ 0 或 读取数 ＜ 1)
        回退多选输入 (数量, 选中数组)
        返回 ()
    .如果真结束
    .如果真 (记录.事件类型 ＝ 1 且 记录.按键按下 ≠ 0)
        键码 ＝ 记录.虚拟键码
        .判断开始 (键码 ＝ 38)
            .如果真 (光标 ＞ 1)
                光标 ＝ 光标 － 1
                .如果真 (重绘多选 (行, 光标, 勾选, 数量, 选项数组) ＝ 假)
                    回退多选输入 (数量, 选中数组)
                    返回 ()
                .如果真结束

            .如果真结束

        .判断 (键码 ＝ 40)
            .如果真 (光标 ＜ 数量)
                光标 ＝ 光标 ＋ 1
                .如果真 (重绘多选 (行, 光标, 勾选, 数量, 选项数组) ＝ 假)
                    回退多选输入 (数量, 选中数组)
                    返回 ()
                .如果真结束

            .如果真结束

        .判断 (键码 ＝ 32)
            .如果 (勾选[光标])
                勾选[光标] ＝ 假
            .否则
                勾选[光标] ＝ 真
            .如果结束
            .如果真 (重绘多选 (行, 光标, 勾选, 数量, 选项数组) ＝ 假)
                回退多选输入 (数量, 选中数组)
                返回 ()
            .如果真结束

        .判断 (键码 ＝ 13)
            完成 ＝ 真
        .判断 (键码 ＝ 27)
            光标 ＝ 0
            完成 ＝ 真
        .默认

        .判断结束

    .如果真结束

.判断循环尾 ()
FlushConsoleInputBuffer (GetStdHandle (#控制台_标准输入))
.如果真 (光标 ＞ 0)
    .计次循环首 (数量, i)
        .如果真 (勾选[i])
            加入成员 (选中数组, i)
        .如果真结束

    .计次循环尾 ()
.如果真结束
置光标位置 (0, 行 ＋ 数量)

.子程序 提示输入密码, 文本型, , 隐藏回显的密码输入（非控制台环境直接读取）
.参数 提示语, 文本型
.局部变量 输入句柄, 整数型
.局部变量 原模式, 整数型
.局部变量 有控制台, 逻辑型
.局部变量 输入, 文本型

设置控制台属性 (#控制台_亮青)
写控制台 (“? ”)
设置控制台属性 (#控制台_白)
写控制台 (提示语)
设置控制台属性 (#控制台_灰)
写控制台 (“: ”)
输入句柄 ＝ GetStdHandle (#控制台_标准输入)
有控制台 ＝ 假
.如果真 (GetConsoleMode (输入句柄, 原模式) ≠ 0)
    有控制台 ＝ 真
    SetConsoleMode (输入句柄, 原模式 － 位与 (原模式, 4))
.如果真结束
输入 ＝ 读取输入行 ()
.如果真 (有控制台)
    SetConsoleMode (输入句柄, 原模式)
    写控制台 (#换行符)
.如果真结束
返回 (输入)

.子程序 输出装饰行, , , 输出 VT 装饰样式行；装饰码可组合，如 1 粗体 3 斜体 4 下划线 9 删除线，不支持时原样输出
.参数 文本, 文本型
.参数 装饰码, 文本型

.如果 (g_VT支持)
    写控制台 (字符 (27) ＋ “[” ＋ 装饰码 ＋ “m” ＋ 文本 ＋ 字符 (27) ＋ “[0m”)
    写控制台 (#换行符)
.否则
    写控制台 (文本)
    写控制台 (#换行符)
.如果结束


.子程序 输出日历, , , 输出月历（年 4 位、月 1-12），星期列自周日起、固定 6 行高度；高亮日可空：省略时自动高亮“今天”（仅当所示月份为当前月），0 或越界则无高亮
.参数 年, 整数型
.参数 月, 整数型
.参数 高亮日, 整数型, 可空
.局部变量 各月天数, 整数型, , "0"
.局部变量 闰年, 逻辑型
.局部变量 天数, 整数型
.局部变量 年2, 整数型
.局部变量 月2, 整数型
.局部变量 世纪, 整数型
.局部变量 年尾, 整数型
.局部变量 h, 整数型
.局部变量 偏移, 整数型
.局部变量 单元格, 文本型
.局部变量 日, 整数型
.局部变量 列位, 整数型
.局部变量 i, 整数型
.局部变量 高亮值, 整数型
.局部变量 补行, 整数型

.如果真 (月 ＜ 1 或 月 ＞ 12)
    返回 ()
.如果真结束
清除数组 (各月天数)
加入成员 (各月天数, 31)
加入成员 (各月天数, 28)
加入成员 (各月天数, 31)
加入成员 (各月天数, 30)
加入成员 (各月天数, 31)
加入成员 (各月天数, 30)
加入成员 (各月天数, 31)
加入成员 (各月天数, 31)
加入成员 (各月天数, 30)
加入成员 (各月天数, 31)
加入成员 (各月天数, 30)
加入成员 (各月天数, 31)
闰年 ＝ 假
.如果真 (取整 (年 ÷ 4) × 4 ＝ 年)
    闰年 ＝ 真
.如果真结束
.如果真 (取整 (年 ÷ 100) × 100 ＝ 年)
    闰年 ＝ 假
.如果真结束
.如果真 (取整 (年 ÷ 400) × 400 ＝ 年)
    闰年 ＝ 真
.如果真结束
.如果真 (闰年)
    各月天数[2] ＝ 29
.如果真结束
天数 ＝ 各月天数[月]
高亮值 ＝ 0
.如果 (是否为空 (高亮日))
    .如果真 (年 ＝ 取年份 (取现行时间 ()) 且 月 ＝ 取月份 (取现行时间 ()))
        高亮值 ＝ 取日 (取现行时间 ())
    .如果真结束

.否则
    高亮值 ＝ 高亮日
.如果结束
.如果真 (高亮值 ＜ 1 或 高亮值 ＞ 天数)
    高亮值 ＝ 0
.如果真结束
年2 ＝ 年
月2 ＝ 月
.如果 (月2 ＜ 3)
    月2 ＝ 月2 ＋ 12
    年2 ＝ 年2 － 1
.否则

.如果结束
世纪 ＝ 取整 (年2 ÷ 100)
年尾 ＝ 年2 － 世纪 × 100
h ＝ 1 ＋ 取整 (13 × (月2 ＋ 1) ÷ 5) ＋ 年尾 ＋ 取整 (年尾 ÷ 4) ＋ 取整 (世纪 ÷ 4) － 2 × 世纪
h ＝ h － 取整 (h ÷ 7) × 7
.如果真 (h ＜ 0)
    h ＝ h ＋ 7
.如果真结束
偏移 ＝ h ＋ 6
偏移 ＝ 偏移 － 取整 (偏移 ÷ 7) × 7
设置控制台属性 (#控制台_白)
写控制台 (到文本 (年) ＋ “ 年 ” ＋ 到文本 (月) ＋ “ 月”)
写控制台 (#换行符)
设置控制台属性 (#控制台_亮青)
写控制台 (“ 日 一 二 三 四 五 六”)
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)
列位 ＝ 0
.计次循环首 (偏移, i)
    写控制台 (“   ”)
    列位 ＝ 列位 ＋ 1
.计次循环尾 ()
.计次循环首 (天数, 日)
    单元格 ＝ 到文本 (日)
    单元格 ＝ “ ” ＋ 取重复文本 (2 － 取文本长度 (单元格), “ ”) ＋ 单元格
    .如果 (日 ＝ 高亮值)
        设置控制台属性 (#控制台_亮蓝)
        写控制台 (单元格)
        设置控制台属性 (#控制台_灰)
    .否则
        写控制台 (单元格)
    .如果结束
    列位 ＝ 列位 ＋ 1
    .如果真 (列位 ＝ 7)
        写控制台 (#换行符)
        列位 ＝ 0
    .如果真结束

.计次循环尾 ()
.如果真 (列位 ＞ 0)
    写控制台 (#换行符)
.如果真结束
补行 ＝ 6 － 取整 ((偏移 ＋ 天数 ＋ 6) ÷ 7)
.计次循环首 (补行, i)
    写控制台 (#换行符)
.计次循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 取大字各行, , , 生成大字图形文本的 5 行图形；支持 A-Z 0-9 与 .-?!，其余字符按空白处理
.参数 文本, 文本型
.参数 各行, 文本型, 参考 数组, 输出 5 行图形文本（█ 与空格组成）
.参数 图案行, 文本型, 参考 数组, 输出 5 行 ASCII 图案（.#. 组成，供逐字上色使用）
.局部变量 字样, 文本型, , "0"
.局部变量 内容, 文本型
.局部变量 行号, 整数型
.局部变量 i, 整数型
.局部变量 码, 整数型
.局部变量 索引, 整数型
.局部变量 行组, 文本型, , "0"
.局部变量 段, 文本型
.局部变量 片段, 文本型
.局部变量 k, 整数型
.局部变量 行文本, 文本型
.局部变量 字符文本, 文本型
.局部变量 图案文本, 文本型

清除数组 (各行)
清除数组 (图案行)
清除数组 (字样)
加入成员 (字样, “.#.,#.#,###,#.#,#.#”)
加入成员 (字样, “##.,#.#,##.,#.#,##.”)
加入成员 (字样, “.##,#..,#..,#..,.##”)
加入成员 (字样, “##.,#.#,#.#,#.#,##.”)
加入成员 (字样, “###,#..,##.,#..,###”)
加入成员 (字样, “###,#..,##.,#..,#..”)
加入成员 (字样, “.##,#..,#.#,#.#,.##”)
加入成员 (字样, “#.#,#.#,###,#.#,#.#”)
加入成员 (字样, “###,.#.,.#.,.#.,###”)
加入成员 (字样, “..#,..#,..#,#.#,.#.”)
加入成员 (字样, “#.#,#.#,##.,#.#,#.#”)
加入成员 (字样, “#..,#..,#..,#..,###”)
加入成员 (字样, “#.#,###,###,#.#,#.#”)
加入成员 (字样, “#.#,###,#.#,#.#,#.#”)
加入成员 (字样, “.#.,#.#,#.#,#.#,.#.”)
加入成员 (字样, “##.,#.#,##.,#..,#..”)
加入成员 (字样, “.#.,#.#,#.#,#.#,##.”)
加入成员 (字样, “##.,#.#,##.,#.#,#.#”)
加入成员 (字样, “.##,#..,.#.,..#,##.”)
加入成员 (字样, “###,.#.,.#.,.#.,.#.”)
加入成员 (字样, “#.#,#.#,#.#,#.#,###”)
加入成员 (字样, “#.#,#.#,#.#,#.#,.#.”)
加入成员 (字样, “#.#,#.#,###,###,#.#”)
加入成员 (字样, “#.#,#.#,.#.,#.#,#.#”)
加入成员 (字样, “#.#,#.#,.#.,.#.,.#.”)
加入成员 (字样, “###,..#,.#.,#..,###”)
加入成员 (字样, “.#.,#.#,#.#,#.#,.#.”)
加入成员 (字样, “.#.,##.,.#.,.#.,###”)
加入成员 (字样, “##.,..#,.#.,#..,###”)
加入成员 (字样, “##.,..#,.#.,..#,##.”)
加入成员 (字样, “#.#,#.#,###,..#,..#”)
加入成员 (字样, “###,#..,##.,..#,##.”)
加入成员 (字样, “.##,#..,##.,#.#,.##”)
加入成员 (字样, “###,..#,.#.,#..,#..”)
加入成员 (字样, “.#.,#.#,.#.,#.#,.#.”)
加入成员 (字样, “.##,#.#,.##,..#,.##”)
加入成员 (字样, “...,...,...,...,.#.”)
加入成员 (字样, “...,...,###,...,...”)
加入成员 (字样, “##.,..#,.#.,...,.#.”)
加入成员 (字样, “.#.,.#.,.#.,...,.#.”)
加入成员 (字样, “...,...,...,...,...”)
内容 ＝ 到大写 (文本)
.计次循环首 (5, 行号)
    行文本 ＝ “”
    图案文本 ＝ “”
    .计次循环首 (取文本长度 (内容), i)
        码 ＝ 取代码 (内容, i)
        索引 ＝ 41
        .如果真 (码 ≥ 65 且 码 ≤ 90)
            索引 ＝ 码 － 64
        .如果真结束
        .如果真 (码 ≥ 48 且 码 ≤ 57)
            索引 ＝ 码 － 48 ＋ 27
        .如果真结束
        .如果真 (码 ＝ 46)
            索引 ＝ 37
        .如果真结束
        .如果真 (码 ＝ 45)
            索引 ＝ 38
        .如果真结束
        .如果真 (码 ＝ 63)
            索引 ＝ 39
        .如果真结束
        .如果真 (码 ＝ 33)
            索引 ＝ 40
        .如果真结束
        行组 ＝ 分割文本 (字样[索引], “,”, )
        .如果 (行号 ≤ 取数组成员数 (行组))
            段 ＝ 行组[行号]
        .否则
            段 ＝ “...”
        .如果结束
        片段 ＝ “”
        .计次循环首 (取文本长度 (段), k)
            字符文本 ＝ 取文本中间 (段, k, 1)
            .如果 (字符文本 ＝ “#”)
                片段 ＝ 片段 ＋ “█”
            .否则
                片段 ＝ 片段 ＋ “ ”
            .如果结束

        .计次循环尾 ()
        行文本 ＝ 行文本 ＋ 片段 ＋ “ ”
        图案文本 ＝ 图案文本 ＋ 段 ＋ “ ”
    .计次循环尾 ()
    加入成员 (各行, 行文本)
    加入成员 (图案行, 图案文本)
.计次循环尾 ()

.子程序 输出大字, , , 输出 ASCII 艺术大字标题（单色；支持 A-Z 0-9 与 .-?!，其余字符按空白处理）
.参数 文本, 文本型
.参数 颜色, 整数型, 可空
.局部变量 色值, 整数型
.局部变量 各行, 文本型, , "0"
.局部变量 图案行, 文本型, , "0"
.局部变量 i, 整数型

色值 ＝ #控制台_亮紫
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
取大字各行 (文本, 各行, 图案行)
设置控制台属性 (色值)
.计次循环首 (取数组成员数 (各行), i)
    写控制台 (各行[i])
    写控制台 (#换行符)
.计次循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 输出渐层大字, , , 输出逐列真彩色渐变大字；颜色文本形如 #FF8800（可省略 #，大小写均可），解析失败回退灰色；非 VT 环境回退为起色的近似单色大字
.参数 文本, 文本型
.参数 起色文本, 文本型
.参数 终色文本, 文本型
.局部变量 起红, 整数型
.局部变量 起绿, 整数型
.局部变量 起蓝, 整数型
.局部变量 终红, 整数型
.局部变量 终绿, 整数型
.局部变量 终蓝, 整数型
.局部变量 各行, 文本型, , "0"
.局部变量 图案行, 文本型, , "0"
.局部变量 行号, 整数型
.局部变量 列号, 整数型
.局部变量 列数, 整数型
.局部变量 比例, 双精度小数型
.局部变量 红, 整数型
.局部变量 绿, 整数型
.局部变量 蓝, 整数型
.局部变量 行文本, 文本型
.局部变量 行缓冲, 文本型
.局部变量 字符文本, 文本型

.如果真 (十六进制色到分量 (起色文本, 起红, 起绿, 起蓝) ＝ 假)
    起红 ＝ 128
    起绿 ＝ 128
    起蓝 ＝ 128
.如果真结束
.如果真 (十六进制色到分量 (终色文本, 终红, 终绿, 终蓝) ＝ 假)
    终红 ＝ 128
    终绿 ＝ 128
    终蓝 ＝ 128
.如果真结束
.如果真 (g_VT支持 ＝ 假)
    输出大字 (文本, RGB到属性 (起红, 起绿, 起蓝))
    返回 ()
.如果真结束
取大字各行 (文本, 各行, 图案行)
.计次循环首 (取数组成员数 (图案行), 行号)
    行文本 ＝ 图案行[行号]
    列数 ＝ 取文本长度 (行文本)
    行缓冲 ＝ “”
    .计次循环首 (列数, 列号)
        .如果 (列数 ≤ 1)
            比例 ＝ 0
        .否则
            比例 ＝ 到数值 (列号 － 1) ÷ 到数值 (列数 － 1)
        .如果结束
        红 ＝ 起红 ＋ 到整数 ((终红 － 起红) × 比例)
        绿 ＝ 起绿 ＋ 到整数 ((终绿 － 起绿) × 比例)
        蓝 ＝ 起蓝 ＋ 到整数 ((终蓝 － 起蓝) × 比例)
        字符文本 ＝ 取文本中间 (行文本, 列号, 1)
        .如果 (字符文本 ＝ “#”)
            行缓冲 ＝ 行缓冲 ＋ 字符 (27) ＋ “[38;2;” ＋ 到文本 (红) ＋ “;” ＋ 到文本 (绿) ＋ “;” ＋ 到文本 (蓝) ＋ “m█”
        .否则
            行缓冲 ＝ 行缓冲 ＋ “ ”
        .如果结束

    .计次循环尾 ()
    写控制台 (行缓冲 ＋ 字符 (27) ＋ “[0m”)
    写控制台 (#换行符)
.计次循环尾 ()
设置控制台属性 (#控制台_灰)

.子程序 输出散点, , , 输出散点画布（在 宽×高 字符网格上绘制 X/Y 点集）
.参数 宽, 整数型
.参数 高, 整数型
.参数 X数组, 整数型, 数组
.参数 Y数组, 整数型, 数组
.参数 标记, 文本型, 可空
.参数 颜色, 整数型, 可空
.局部变量 网格, 文本型, , "0"
.局部变量 标记值, 文本型
.局部变量 色值, 整数型
.局部变量 数量, 整数型
.局部变量 i, 整数型
.局部变量 列号, 整数型
.局部变量 行号, 整数型

.如果真 (宽 ≤ 0 或 高 ≤ 0)
    返回 ()
.如果真结束
.如果真 (宽 ＞ 200)
    宽 ＝ 200
.如果真结束
.如果真 (高 ＞ 100)
    高 ＝ 100
.如果真结束
标记值 ＝ “*”
.如果真 (是否为空 (标记) ＝ 假 且 标记 ≠ “”)
    标记值 ＝ 取文本左边 (标记, 1)
.如果真结束
色值 ＝ #控制台_亮绿
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
重定义数组 (网格, 假, 宽 × 高)
数量 ＝ 取数组成员数 (X数组)
.如果真 (取数组成员数 (Y数组) ＜ 数量)
    数量 ＝ 取数组成员数 (Y数组)
.如果真结束
.变量循环首 (1, 数量, 1, i)
    .如果真 (X数组[i] ≥ 1 且 X数组[i] ≤ 宽 且 Y数组[i] ≥ 1 且 Y数组[i] ≤ 高)
        网格[(Y数组[i] － 1) × 宽 ＋ X数组[i]] ＝ 标记值
    .如果真结束

.变量循环尾 ()
设置控制台属性 (#控制台_深灰)
写控制台 (“┌” ＋ 取重复文本 (宽, “─”) ＋ “┐”)
写控制台 (#换行符)
.变量循环首 (1, 高, 1, 行号)
    设置控制台属性 (#控制台_深灰)
    写控制台 (“│”)
    设置控制台属性 (色值)
    .变量循环首 (1, 宽, 1, 列号)
        .如果 (网格[(行号 － 1) × 宽 ＋ 列号] ＝ “”)
            写控制台 (“ ”)
        .否则
            写控制台 (网格[(行号 － 1) × 宽 ＋ 列号])
        .如果结束

    .变量循环尾 ()
    设置控制台属性 (#控制台_深灰)
    写控制台 (“│”)
    写控制台 (#换行符)
.变量循环尾 ()
写控制台 (“└” ＋ 取重复文本 (宽, “─”) ＋ “┘”)
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)

.子程序 输出面包屑, , , 输出路径面包屑（首段为根节点高亮，其余为灰色节点，箭头分隔）
.参数 段数组, 文本型, 数组
.参数 颜色, 整数型, 可空
.局部变量 数量, 整数型
.局部变量 色值, 整数型
.局部变量 i, 整数型

数量 ＝ 取数组成员数 (段数组)
.如果真 (数量 ≤ 0)
    返回 ()
.如果真结束
色值 ＝ #控制台_亮青
.如果真 (是否为空 (颜色) ＝ 假)
    色值 ＝ 颜色
.如果真结束
.计次循环首 (数量, i)
    .如果 (i ＞ 1)
        设置控制台属性 (#控制台_深灰)
        写控制台 (“ → ”)
        设置控制台属性 (#控制台_灰)
    .否则
        设置控制台属性 (色值)
    .如果结束
    写控制台 (段数组[i])
.计次循环尾 ()
写控制台 (#换行符)
设置控制台属性 (#控制台_灰)

' 演示入口“运行Aurora演示”及分节演示已拆分到 程序集_测试（演示_标记样式 … 演示_控制台信息）
' 分节演示已迁移至 程序集_测试；渲染库实现保留在本程序集
