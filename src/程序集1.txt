.版本 2

.程序集 程序集1
.程序集变量 g_输出句柄, 整数型, , , 标准输出设备句柄缓存

.子程序 _启动子程序, 整数型, , 本子程序在程序启动后最先执行

_临时子程序 ()
返回 (0)  ' 可以根据您的需要返回任意数值

.子程序 _临时子程序
.局部变量 参数组, 文本型, , "0"
.局部变量 i, 整数型
.局部变量 诊断, 逻辑型

取命令行 (参数组)
.计次循环首 (取数组成员数 (参数组), i)
    .如果真 (寻找文本 (参数组[i], “静态”, , 假) ≠ -1)
        g_动效模式 ＝ 1
    .如果真结束
    .如果真 (寻找文本 (参数组[i], “动态”, , 假) ≠ -1)
        g_动效模式 ＝ 2
    .如果真结束
    .如果真 (寻找文本 (参数组[i], “诊断”, , 假) ≠ -1)
        诊断 ＝ 真
    .如果真结束
    .如果真 (寻找文本 (参数组[i], “diag”, , 假) ≠ -1)
        诊断 ＝ 真
    .如果真结束

.计次循环尾 ()
.如果真 (诊断)
    诊断输出测试 ()
    返回 ()
.如果真结束
运行Aurora演示 ()

.子程序 诊断输出测试, , , 控制台输出诊断：分别验证 VT 关闭/开启时的定位与直写行为并写入报告文件
.局部变量 句柄, 整数型
.局部变量 写出数, 整数型
.局部变量 列, 整数型
.局部变量 行, 整数型
.局部变量 列2, 整数型
.局部变量 行2, 整数型
.局部变量 目标行, 整数型
.局部变量 结果, 整数型
.局部变量 参数组, 文本型, , "0"
.局部变量 j, 整数型
.局部变量 报告, 文本型
.局部变量 k, 整数型
.局部变量 缓冲, 文本型
.局部变量 读回数, 整数型
.局部变量 计数, 整数型

句柄 ＝ 取输出句柄 ()
取命令行 (参数组)
报告 ＝ “== 诊断报告 ==” ＋ #换行符
.计次循环首 (取数组成员数 (参数组), j)
    报告 ＝ 报告 ＋ “参数” ＋ 到文本 (j) ＋ “＝[” ＋ 参数组[j] ＋ “]” ＋ #换行符
.计次循环尾 ()
报告 ＝ 报告 ＋ “输出句柄＝” ＋ 到文本 (句柄) ＋ “，输入句柄＝” ＋ 到文本 (GetStdHandle (#控制台_标准输入)) ＋ #换行符
报告 ＝ 报告 ＋ “阶段一（VT 关闭）可交互＝” ＋ 到文本 (控制台可交互 ()) ＋ “，VT支持＝” ＋ 到文本 (g_VT支持) ＋ #换行符
取光标位置 (列, 行)
报告 ＝ 报告 ＋ “初始光标＝” ＋ 到文本 (列) ＋ “,” ＋ 到文本 (行) ＋ “，控制台宽＝” ＋ 到文本 (取控制台宽度 ()) ＋ #换行符
目标行 ＝ 行 － 1
.如果真 (目标行 ＜ 0)
    目标行 ＝ 行 ＋ 1
.如果真结束
报告 ＝ 报告 ＋ “置光标(0,” ＋ 到文本 (目标行) ＋ “)返回＝” ＋ 到文本 (置光标位置 (0, 目标行)) ＋ #换行符
取光标位置 (列, 行)
报告 ＝ 报告 ＋ “读回光标＝” ＋ 到文本 (列) ＋ “,” ＋ 到文本 (行) ＋ #换行符
写出数 ＝ 0
置光标位置 (0, 行)
结果 ＝ WriteConsoleA (句柄, “BBBBBBBBBB”, 10, 写出数, 0)
取光标位置 (列2, 行2)
报告 ＝ 报告 ＋ “直写返回＝” ＋ 到文本 (结果) ＋ “，写出数＝” ＋ 到文本 (写出数) ＋ “，直写后光标＝” ＋ 到文本 (列2) ＋ “,” ＋ 到文本 (行2) ＋ #换行符
报告 ＝ 报告 ＋ “启用VT返回＝” ＋ 到文本 (启用虚拟终端 ()) ＋ “，VT支持＝” ＋ 到文本 (g_VT支持) ＋ #换行符
取光标位置 (列, 行)
目标行 ＝ 行 － 1
.如果真 (目标行 ＜ 0)
    目标行 ＝ 行 ＋ 1
.如果真结束
报告 ＝ 报告 ＋ “VT后置光标(0,” ＋ 到文本 (目标行) ＋ “)返回＝” ＋ 到文本 (置光标位置 (0, 目标行)) ＋ #换行符
取光标位置 (列, 行)
报告 ＝ 报告 ＋ “VT后读回光标＝” ＋ 到文本 (列) ＋ “,” ＋ 到文本 (行) ＋ #换行符
写出数 ＝ 0
置光标位置 (0, 行)
结果 ＝ WriteConsoleA (句柄, “DDDDDDDDDD”, 10, 写出数, 0)
取光标位置 (列2, 行2)
报告 ＝ 报告 ＋ “VT后直写返回＝” ＋ 到文本 (结果) ＋ “，写出数＝” ＋ 到文本 (写出数) ＋ “，VT后直写后光标＝” ＋ 到文本 (列2) ＋ “,” ＋ 到文本 (行2) ＋ #换行符
.计次循环首 (40, k)
    写控制台 (“填充行” ＋ 到文本 (k) ＋ #换行符)
.计次循环尾 ()
取光标位置 (列, 行)
报告 ＝ 报告 ＋ “填充40行后光标＝” ＋ 到文本 (列) ＋ “,” ＋ 到文本 (行) ＋ #换行符
.计次循环首 (6, k)
    置光标位置 (0, 行)
    写控制台 (“帧” ＋ 到文本 (k) ＋ “-甲” ＋ #换行符)
    写控制台 (“帧” ＋ 到文本 (k) ＋ “-乙” ＋ #换行符)
    写控制台 (“帧” ＋ 到文本 (k) ＋ “-丙” ＋ #换行符)
.计次循环尾 ()
取光标位置 (列2, 行2)
报告 ＝ 报告 ＋ “底部复刻后光标＝” ＋ 到文本 (列2) ＋ “,” ＋ 到文本 (行2) ＋ #换行符
目标行 ＝ 行 － 2
.如果真 (目标行 ＜ 0)
    目标行 ＝ 0
.如果真结束
缓冲 ＝ 取空白文本 (40)
读回数 ＝ 0
ReadConsoleOutputCharacterA (句柄, 缓冲, 40, 目标行 × 65536, 读回数)
报告 ＝ 报告 ＋ “行-2读回＝[” ＋ 取文本左边 (缓冲, 40) ＋ “]” ＋ #换行符
缓冲 ＝ 取空白文本 (40)
读回数 ＝ 0
ReadConsoleOutputCharacterA (句柄, 缓冲, 40, 行 × 65536, 读回数)
报告 ＝ 报告 ＋ “行读回＝[” ＋ 取文本左边 (缓冲, 40) ＋ “]” ＋ #换行符
写控制台 (“[调用前标记]” ＋ #换行符)
取光标位置 (列, 行)
演示_进度条 ()
演示_加载动画 ()
演示_多进度组 ()
演示_日历 ()
取光标位置 (列2, 行2)
报告 ＝ 报告 ＋ “调用演示后光标＝” ＋ 到文本 (列2) ＋ “,” ＋ 到文本 (行2) ＋ #换行符
.计次循环首 (60, k)
    缓冲 ＝ 取空白文本 (24)
    读回数 ＝ 0
    ReadConsoleOutputCharacterA (句柄, 缓冲, 24, (行 ＋ k － 1) × 65536, 读回数)
    缓冲 ＝ 删首尾空 (缓冲)
    .如果真 (取文本长度 (缓冲) ＞ 0)
        报告 ＝ 报告 ＋ “行” ＋ 到文本 (行 ＋ k － 1) ＋ “=[” ＋ 缓冲 ＋ “]” ＋ #换行符
    .如果真结束

.计次循环尾 ()
写到文件 (“D:\git\e-packager\eproj\pack\diag_report.txt”, 到字节集 (报告))
写控制台 (报告)
写控制台 (“== 诊断结束（8 秒后自动退出） ==” ＋ #换行符)
延时 (8000)

.子程序 输出行
.参数 s, 文本型

输出调试文本 (s)
写控制台 (s)
写控制台 (#换行符)

.子程序 取输出句柄, 整数型, , 取标准输出设备句柄，带缓存

.如果 (g_输出句柄 ＝ 0)
    g_输出句柄 ＝ GetStdHandle (#控制台_标准输出)
.否则

.如果结束
返回 (g_输出句柄)

.子程序 写控制台, , , 输出文本：优先直写控制台缓冲（与光标定位共用同一游标，支持原地刷新），非控制台环境自动回退标准输出
.参数 文本, 文本型
.局部变量 句柄, 整数型
.局部变量 写出数, 整数型
.局部变量 字节数, 整数型

句柄 ＝ 取输出句柄 ()
字节数 ＝ 取文本长度 (文本)
.如果真 (句柄 ≠ 0 且 字节数 ＞ 0 且 WriteConsoleA (句柄, 文本, 字节数, 写出数, 0) ≠ 0)
    返回 ()
.如果真结束
标准输出 (, 文本)

.子程序 置文本颜色, 逻辑型, 公开, 设置控制台前景/背景颜色，成功返回真
.参数 前景色, 整数型, , 0-15，可用 #控制台_黑 到 #控制台_白 常量
.参数 背景色, 整数型, 可空, 0-15，省略时保持当前背景色不变
.局部变量 属性值, 整数型

.如果真 (是否为空 (背景色))
    背景色 ＝ -1
.如果真结束
.如果 (背景色 ＝ -1)
    属性值 ＝ 前景色
.否则
    属性值 ＝ 前景色 ＋ 左移 (背景色, #控制台_背景位移)
.如果结束
.如果 (SetConsoleTextAttribute (取输出句柄 (), 属性值) ＝ 0)
    返回 (假)
.否则
    返回 (真)
.如果结束


.子程序 彩色输出行, 逻辑型, 公开, 以指定颜色输出一行文本并换行，成功返回真
.参数 s, 文本型, , 欲输出的文本
.参数 前景色, 整数型, , 0-15，可用 #控制台_黑 到 #控制台_白 常量
.参数 背景色, 整数型, 可空, 0-15，省略时使用默认背景

.如果真 (置文本颜色 (前景色, 背景色) ＝ 假)
    返回 (假)
.如果真结束
写控制台 (s)
写控制台 (#换行符)
返回 (置文本颜色 (#控制台_灰, ))

.子程序 测试彩色输出, , , 验证用入口：演示各种颜色输出

输出行 (“普通输出：Hello！”)
彩色输出行 (“红色文本：错误提示示例”, #控制台_亮红, )
彩色输出行 (“绿色文本：成功提示示例”, #控制台_亮绿, )
彩色输出行 (“黄色文本：警告提示示例”, #控制台_亮黄, )
彩色输出行 (“蓝色文本：信息提示示例”, #控制台_亮蓝, )
彩色输出行 (“白字红底：高亮示例”, #控制台_白, #控制台_深红)
彩色输出行 (“恢复默认颜色”, #控制台_灰, )
